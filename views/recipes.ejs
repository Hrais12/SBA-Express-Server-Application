
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/public/css/style.css">
    <title>Recipes</title>
</head>
<body>
    <h1>TastyTable:Explore Delicious Recipes</h1>
    <a href="/api/recipes/<%= username %>/newRecipe"><button class="find">Add New Recipes</button></a>
    
    <ul>
        <% recipes.forEach(recipe => { %>
            <div class="recipe">
            <li>
                <h2><%= recipe.title %></h2>
                <p><strong>Category:</strong> <%= recipe.categoryId %></p>
                <p><strong>Description:</strong> <%= recipe.description %></p>
                <p><strong>Ingredients:</strong> <%= recipe.ingredients %></p>
                <p><strong>Instructions:</strong> <%= recipe.instructions %></p>
                <div class="buttonsContainer">
                   <a href="/api/recipes/<%= username %>/<%= recipe.id %>/editRecipe"><button class="find">Edit</button></a> 
                
                    <form class="delete" action="/api/recipes/<%= username %>/<%= recipe.id %>/delete" method="POST">
                        <!-- Use hidden input to pass recipe ID -->
                        <input type="hidden" name="recipeId" value="<%= recipe.id %>">
                        <input type="hidden" name="_method" value="DELETE">
                        <button class="find" type="submit">Delete</button>
                    </form>
                </div>
                
            </li>
        </div>
        <% }); %>
    </ul>
</body>
</html>
